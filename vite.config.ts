import { defineConfig } from 'vite';

export default defineConfig({
  // Relative asset URLs, so the same build works at invigoro.github.io/Stele/,
  // under `vite preview`, or on a custom domain later.
  base: './',
});
